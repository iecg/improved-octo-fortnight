/// <reference types="nativewind/types" />

// The Tailwind entry point is imported for its side effect; Metro turns it
// into style registrations rather than a module with exports.
declare module '*.css';
